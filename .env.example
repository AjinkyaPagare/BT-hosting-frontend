# Frontend environment configuration for Baap Teams video calling
# Point this to the FastAPI backend base URL (without trailing slash)
VITE_API_BASE=http://localhost:8080
# Example for LAN deployment
# VITE_API_BASE=http://172.16.21.118:8080

# Enable axios credentials if backend expects cookies
VITE_API_WITH_CREDENTIALS=false
